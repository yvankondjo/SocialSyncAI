# Configuration Redis pour Message Batching
# Ajouter ces variables à votre .env :

# Redis Configuration (OBLIGATOIRE)
REDIS_URL=redis://redis:6379/0

# Batching Configuration (optionnel - valeurs par défaut)
BATCH_WINDOW_SECONDS=30
CACHE_TTL_HOURS=1
HISTORY_LIMIT=200
SCANNER_INTERVAL=0.5

# Note: Si Redis est dans Docker Compose, utilisez:
# REDIS_URL=redis://redis:6379/0
# 
# Si Redis est local, utilisez:
# REDIS_URL=redis://localhost:6379/0
