# Railway

Railway can host the frontend and backend separately if you provide:

- the correct start command,
- real environment variables,
- external Redis for background processing,
- a managed database layer through Supabase.

The repository does not currently include Railway-specific deployment manifests.
