# Contributing

The main contribution guide lives at the repository root in [`CONTRIBUTING.md`](../../CONTRIBUTING.md).

Before opening a pull request:

1. Run the backend tests from the repo root with `pytest`.
2. Run the frontend checks from `frontend/` with `npm run lint`, `npm run typecheck`, and `npm run build`.
3. Keep documentation aligned with the actual repository contents.
