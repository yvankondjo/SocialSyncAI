# Knowledge Base

The knowledge base feature supports:

- Document ingestion from backend workers and services.
- FAQ-oriented retrieval flows.
- Vector-backed search stored in the application data layer.

See `backend/app/services/retriever.py` and the knowledge document routers for implementation details.
